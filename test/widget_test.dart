// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility that Flutter provides. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets(
    'Login screen text exists',
    (WidgetTester tester) async {
      // Build our app and trigger a frame.
      // await tester.pumpWidget(LiveFeedApp());

      // Verify that login screen components exist
      // expect(find.text('LOGIN'), findsOneWidget);
      // expect(
      //     'Login has never been easier. Just enter your phone number for a one-time password!',
      //     findsOneWidget);
      // expect('Enter your phone number', findsOneWidget);
      // expect('Submit', findsOneWidget);
      // expect('Standard message and data rates may apply', findsOneWidget);
      // expect(
      //     find.text(
      //         '© 2017 - 2020 Live Media Inc. All rights reserved. Patents applied for in the US and abroad. '),
      //     findsOneWidget);

      // Tap the '+' icon and trigger a frame.
      // await tester.tap(find.byIcon(Icons.add));
      // await tester.pump();

      // Verify that our counter has incremented.
      // expect(find.text('0'), findsNothing);
      // expect(find.text('1'), findsOneWidget);
    },
  );
}
